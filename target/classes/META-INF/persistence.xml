<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
             https://jakarta.ee/xml/ns/persistence/persistence_3_1.xsd"
             version="3.1">

  <persistence-unit name="inventoryPU" transaction-type="JTA">
    <jta-data-source>java:/InventoryPostgresDS</jta-data-source>

    <properties>
      <!-- Le schéma (tables, contraintes, index, données de démo) est géré par le script SQL
           fourni (cf. database/01_schema_seed.sql), pas par Hibernate : on ne laisse pas
           hbm2ddl toucher la base. -->
      <property name="hibernate.hbm2ddl.auto" value="none"/>
      <!-- Sans cette stratégie, Hibernate utilise le nom du champ Java tel quel (ex. "fullName")
           comme nom de colonne implicite, alors que le script SQL utilise snake_case
           (ex. "full_name") : sans elle, le mapping ne correspondrait pas au schéma réel. -->
      <property name="hibernate.physical_naming_strategy"
                value="org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy"/>
      <property name="hibernate.show_sql" value="true"/>
      <property name="hibernate.format_sql" value="true"/>
      <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>
    </properties>
  </persistence-unit>

</persistence>
